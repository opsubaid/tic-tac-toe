# 1. Ignore all build artifacts and IDE-specific folders
bin/
obj/

# 2. Ignore specific file extensions generated by compilers or IDEs
*.exe
*.o
*.cbp
*.layout

# 3. Strict allow-list approach
# First, ignore everything in the directory
*

# Allow the .gitignore file itself so your settings are saved
!.gitignore

# Explicitly allow only the files you want to push
!main.cpp
!README.md
!LICENSE